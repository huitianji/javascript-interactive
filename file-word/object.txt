什么是对象？
对象是一个整体，对外提供的一些操作

什么是面向对象？
使用对象时，只关注对象提供的功能，不关注其内部细节
比如jquery

面向对象是一种通用思想，并非只有编程中能用，认识事情都可以用。

对象的组成：
方法：函数
属性：变量

<!----!>
工厂模式
<!----!>

类，对象

类：    模子
对象：  产品

Array   类
arr     对象

Array.push();//错误的写法

/////
用构造函数   加属性
用原型       加方法



用json实现面向对象：优点  -》简单
                    缺点-》不适合多个对象


//命名空间
//在公司里，把同一类的方法，包在一起
var zx = {}
var zx.op = {}
var zx.mp = {}
zx.op.getName = function(){
    ...
}
zx.mp.getName = function(){
    xxx
}




//1
宿主对象（由浏览器提供的对象）
DOM / BOM

本地对象(非静态对象)需要经过new  或者实例化

内置对象(静态对象)--》直接来用的。不用new的都属于静态对象 比如：Math.ceil();    Global






















